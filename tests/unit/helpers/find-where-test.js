import { findWhere } from '../../../helpers/find-where';
import { module, test } from 'qunit';

module('Unit | Helper | find where');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = findWhere(42);
  assert.ok(result);
});
