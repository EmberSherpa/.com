import { videoQuality } from '../../../helpers/video-quality';
import { module, test } from 'qunit';

module('Unit | Helper | video quality');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = videoQuality(42);
  assert.ok(result);
});
