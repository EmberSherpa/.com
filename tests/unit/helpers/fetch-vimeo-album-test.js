import { fetchVimeoAlbum } from '../../../helpers/fetch-vimeo-album';
import { module, test } from 'qunit';

module('Unit | Helper | fetch vimeo album');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = fetchVimeoAlbum(42);
  assert.ok(result);
});
